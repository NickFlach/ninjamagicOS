<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ninjamagic.launcher">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.BODY_SENSORS" />

    <application
        android:name=".NinjaMagicApp"
        android:label="NinjaMagic"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.NinjaMagic"
        android:supportsRtl="true">

        <!-- Main launcher activity -->
        <activity
            android:name=".LauncherActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:stateNotNeeded="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Home screen intent filter -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Agent foreground service -->
        <service
            android:name=".service.AgentService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- MCP server service -->
        <service
            android:name=".service.McpService"
            android:exported="true"
            android:permission="com.ninjamagic.permission.MCP_ACCESS">
            <intent-filter>
                <action android:name="com.ninjamagic.action.MCP_CONNECT" />
            </intent-filter>
        </service>
    </application>
</manifest>
